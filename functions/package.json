{
  "name": "marmitte-functions",
  "version": "0.1.0",
  "description": "Cloud Functions pour La Marmite Express — proxy IA generation images + resize Storage trigger",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "eslint --ext .ts src",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "blurhash": "^2.0.5",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4",
    "vitest": "^2.1.4"
  },
  "private": true
}
